import * as React from 'react';

import type { ReactNavigation } from '../types';

export const ThemeContext = React.createContext<
  ReactNavigation.Theme | undefined
>(undefined);

ThemeContext.displayName = 'ThemeContext';
